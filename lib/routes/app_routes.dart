abstract class Routes {
  static const movieList = Paths.movieList;
  static const movieDetails = Paths.movieDetails;
}
abstract class Paths{
  static const movieList = '/movieList';
  static const movieDetails = '/movieDetails';
}